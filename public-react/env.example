# ==============================
# Public Frontend - ENV Example (Vite)
# ==============================
# Dùng cho phát triển trên máy cá nhân (dev)

# Dev (localhost - chạy cùng docker-compose.yml)
VITE_API_BASE_URL=http://localhost:4000

# ==============================
# Gợi ý cho Production (trên server)
# ==============================
# Khi build bằng docker-compose.ssl.yml trên server,
# biến này sẽ được set từ file .env ở thư mục root:
#   API_BASE_URL=https://api.halocrms.io.vn
#
# docker-compose.ssl.yml đã cấu hình:
#   public:
#     env_file: .env
#     environment:
#       - VITE_API_BASE_URL=${API_BASE_URL:-https://api.halocrms.io.vn}
#
# => Không cần commit giá trị production ở đây.